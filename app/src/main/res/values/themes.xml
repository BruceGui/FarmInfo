<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 界面主题文件 -->

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomActionBarTheme" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/CustomActionBarTheme.ActionBar</item>
        <item name="actionBarStyle">@style/CustomActionBarTheme.ActionBar</item>

        <item name="android:actionMenuTextAppearance">@style/CustomActionBarTheme.ActionBar.Text</item>
        <item name="actionMenuTextAppearance">@style/CustomActionBarTheme.ActionBar.Text</item>

        <item name="android:actionMenuTextColor">@color/greyText</item>
        <item name="actionMenuTextColor">@color/greyText</item>

        <item name="android:actionBarTabTextStyle">@style/CustomActionBarTheme.ActionBar.Text</item>
        <item name="actionBarTabTextStyle">@style/CustomActionBarTheme.ActionBar.Text</item>

        <item name="android:spinnerDropDownItemStyle">@style/DropDownItem</item>
        <item name="spinnerDropDownItemStyle">@style/DropDownItem</item>

        <item name="android:spinnerItemStyle">@style/SpinnerItem</item>
        <item name="android:popupMenuStyle">@style/MyPopupMenu</item>
        <item name="popupMenuStyle">@style/MyPopupMenu</item>

        <item name="android:itemTextAppearance">@style/myCustomMenuTextApearance</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="CustomActionBarTheme.Transparent">
        <item name="android:actionBarStyle">@style/CustomActionBarTheme.ActionBar.Transparent</item>
        <item name="actionBarStyle">@style/CustomActionBarTheme.ActionBar.Transparent</item>

        <item name="android:windowTranslucentStatus">true</item>

        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="CustomActionBarTheme.ActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="android:titleTextStyle">@style/CustomActionBarTheme.ActionBar.TitleTextStyle</item>
        <item name="titleTextStyle">@style/CustomActionBarTheme.ActionBar.TitleTextStyle</item>

        <item name="android:subtitleTextStyle">@style/CustomActionBarTheme.ActionBar.Text</item>
        <item name="subtitleTextStyle">@style/CustomActionBarTheme.ActionBar.Text</item>

        <item name="android:background">@android:color/white</item>
        <item name="background">@android:color/white</item>

        <!--
        <item name="android:displayOptions">showHome|homeAsUp</item>
        <item name="displayOptions">showHome|homeAsUp</item>
        -->

        <item name="android:gravity">left</item>
    </style>

    <style name="CustomActionBarTheme.ActionBar.Transparent" >
        <item name="android:background">@color/transparent_light</item>
        <item name="background">@color/transparent_light</item>
    </style>


    <!--  Actionbar text-->
    <style name="CustomActionBarTheme.ActionBar.Text" parent="@android:style/TextAppearance">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/greyText</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:paddingLeft">20dp</item>
    </style>

    <!-- Styles for the view pager tab strip -->
    <style name="Theme.PageIndicatorDefaults" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/CustomActionBarTheme.ActionBar</item>
        <item name="actionBarStyle">@style/CustomActionBarTheme.ActionBar</item>

    </style>

    <!-- The End of Application Theme! -->

</resources>